package com.buenosdev.tree.binaryTreeLevelReverseOrderTraversal;

public class ProblemSolutionReviewA {

    /*
    * Level Tracking: If the current level is equal to or greater than the size of the list of levels, a new level is added at the beginning of the list. This ensures that the levels are stored in a bottom-up order.
    * Node Visitation: The function then recursively visits the left and right children of the current node, incrementing the level by 1 each time. After both children have been visited, the value of the current node is added to its corresponding level in the list.    *
    * */

    // BFS
    // TODO AUG
}
